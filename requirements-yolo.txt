# --- YOLO 검출 백엔드 (--backend yolo) -------------------------------------
#
# **`--no-deps` 로 넣는다.** 그냥 넣으면 pip 가 torch 를 2.13.0 → 2.9.1 로
# **내리려 하고**, 위의 고정과 부딪혀 ResolutionImpossible 로 죽는다.
# 원인은 ultralytics 의 `torch<2.10,>=1.8.0` — 상한이다.
#
# 그 상한은 보수적인 것이지 실제 비호환이 아니다. **torch 2.13.0+cu126 에서
# 892 프레임 검출을 끝까지 돌려 확인했다**(DiaRUGA 025·031). 그래서 상한을
# 무시하고 torch 를 그대로 둔다. ultralytics 를 올릴 때는 이 확인을 다시 한다.
#
# 그냥 넣으면 GUI 판 `opencv-python` 도 함께 들어와 위의 headless 판을 가리고
# `libxcb.so.1` 로 죽는다. `--no-deps` 면 그것도 안 들어온다.
#
# 아래는 ultralytics 가 실제로 쓰는 것들을 손으로 적은 것이다. numpy·pillow·
# opencv·torch·torchvision 은 위에 이미 있다.
ultralytics==8.4.7
ultralytics-thop==2.1.6
matplotlib==3.11.1
scipy==1.18.0
polars==1.43.2
psutil==7.2.2
requests==2.34.2
PyYAML==6.0.3
# matplotlib·requests 가 끌고 오는 것들. --no-deps 를 쓰므로 여기 적어야 한다
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
