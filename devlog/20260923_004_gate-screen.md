# 잠금 화면을 둔다 — 코드를 한 번 넣으면 그 브라우저는 다시 안 묻는다 (004)

2026-09-23 · sclee · 브랜치 `work/20260923-sclee` · 003 다음. **DiaRUGA 213 과
같은 날 같은 것이다** — 요청·설계·확인의 긴 이야기는 그쪽에 적었고, 여기는
ForGIA 에서 다른 것만 적는다. 코드는 DiaRUGA 의 파일을 이름만 바꿔 옮겼다
(`gate.py` 머리에 출처를 적었다).

`web/viewer/gate.py` · `templates/viewer/gate.html` · `assets/gate/`(GIF·PNG·
`logo.json`) · `tools/gate_logo.py` · `settings.py`(`GATE_CODE`·미들웨어) ·
`urls.py` · `deploy/{srv,test}/env.template` · `deploy/host/smoke.sh` ·
`tests/test_gate.py`(15개).

## 요약

- `FORGIA_GATE_CODE` 가 비면 아무 일도 안 한다. 넣으면 코드를 모르는 요청은
  `/gate/` 로 가고, 맞는 코드를 넣은 브라우저는 서명한 쿠키(`forgia_gate`)를
  받아 다시 안 묻는다. 코드를 바꾸면 모두 다시 묻는다
- `/healthz` 는 잠그지 않는다(DiaRUGA 034). `smoke.sh` 5번은 `.env` 의 코드로
  들어가 쿠키를 받아 쓴다
- 쿠키 경로가 서브경로(`/ForGIA/`)라 같은 오리진의 DiaRUGA 와 서로 안 섞인다

## ForGIA 에서 다른 것

**로고 GIF 의 장면이 다르다** (`tools/gate_logo.py` 의 `scene_forgia`).
부유성 방 여섯이 작은 것부터 하나씩 부풀며 그려짐(나선을 따라 자라는 순서) →
가시 일곱이 바깥으로 뻗음 → 저서성 사슬이 작은 방 쪽에서 큰 방 쪽으로 번짐 →
글자가 하나씩 떠오름 → 밑줄 → 빛 띠. 2.44초 · 62장 · 629 KB.

사용자가 가져온 GIF(`N:\DiaRUGA\Diadiction\temp\forgia_build_animation.gif`)는
부유성 마크가 **속이 찬 원**이고 글꼴이 Montserrat 이라 원본 PNG(테두리 원 +
가시 · DejaVu Bold)와 끝 장면이 달랐다 — 그래서 원본 PNG 에서 새로 구웠다.

**바탕색은 `#0a0a0f` 다.** 원본 PNG 가 이 색으로 칠해진 불투명 그림이라
(DiaRUGA 는 투명) 잠금 화면 바탕을 거기 맞췄다. 뷰어 어두운 테마의 `--bg`
(`#0f0e15`)와 다르다. 강조색은 로고의 `#c4b5fd`.

## 확인

- `test_gate.py` 15개 — 머리말의 넷을 하나씩 되살려 해당 시험이 실패하는 것을 봤다
- 전체 `--exclude-tag browser` 709개 통과
- 백업 사본(`ForGIA_20260923_162502.db`)에 개발 서버를 붙여 Chromium 으로
  눌러 봤다 — 잠금 화면 → GIF 가 2.65초에 PNG 로 바뀜 → 틀린 코드 문구 →
  맞는 코드로 목록 → 새 탭에서 안 물음 · 375px 폭 가로 넘침 없음 · 콘솔 오류 없음

못 막는 것(계정이 아니다 · 평문 HTTP)은 DiaRUGA 213 과 같다.

## 켜는 법 (배포 뒤)

`/srv/ForGIA/.env` 에 `FORGIA_GATE_CODE=<코드>` 를 넣고 `docker compose up -d web`.
